# Domain models live here.
#
# The inventory data model is still being designed -- see the DESIGN beads
# (`bd list`) and docs/architecture.md for the intended shape. Nothing is
# modelled yet on purpose, so the first schema is designed rather than
# accreted.
