# Infrastructure

`helm/inventory-tng` is the deployment chart. It targets standard Kubernetes and
[CodeNOW](https://www.codenow.com/) from the same templates.

The procedure, values, secrets, and troubleshooting are documented once, in
**[docs/deployment.md](../docs/deployment.md)**. Please add to that file rather
than to this one.
