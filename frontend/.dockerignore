node_modules/
dist/
.vite/
coverage/
# Playwright traces and screenshots. Large, binary, and rewritten by every
# local integration run, so they would bust the build cache for nothing.
test-results/
playwright-report/
